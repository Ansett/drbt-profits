# nginx config template — ${DOMAIN} is substituted at runtime by nginx:alpine's envsubst
# Placed in /etc/nginx/templates/default.conf.template

# HTTP — ACME challenge + redirect to HTTPS
server {
    listen 80;
    server_name ${DOMAIN};

    root /usr/share/nginx/html;
    index index.html;

    # MCP server — reverse proxy
    location /mcp {
        proxy_pass http://mcp:3100;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        # SSE support
        proxy_set_header Connection '';
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 86400s;
    }

    # API — reverse proxy to the Node process (keys + ath-mc.json file)
    location /api/ {
        proxy_pass http://mcp:3100;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Static assets — long cache
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff2?)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    # Bearer-only MCP: do not serve the SPA for OAuth discovery probes.
    # Cursor treats a 200 here as OAuth metadata and can ignore static Authorization headers.
    location ^~ /.well-known/oauth-authorization-server {
        default_type application/json;
        return 404 '{"error":"not_found"}';
    }
    location ^~ /.well-known/oauth-protected-resource {
        default_type application/json;
        return 404 '{"error":"not_found"}';
    }

    # Vue SPA — history mode fallback
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Block access to sensitive files
    location ~ /\.(?!well-known).* {
        deny all;
        access_log off;
        log_not_found off;
    }
}
