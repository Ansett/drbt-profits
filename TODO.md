
Slippage by snipes
  nouvel objet dans le state { 10: 5, 20: 10, ...}
  state.gweiDelta reste utilisé pour block3+
  un champ par clef de l'objet

Utiliser eth price du XLS

Recalculer mes targets

Cancel le calcul de TARGET quand maj de champs (TargetFinder ou pas)

git push --force --set-upstream origin master
